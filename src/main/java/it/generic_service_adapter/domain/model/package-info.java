/**
 * Internal model shared across layers ({@code UserAccountRecord}, {@code WalletMovementRecord},
 * {@code Money}, {@code ErrorCategory}, {@code ProcessingContext}): the only type allowed to cross
 * the layer boundaries.
 */
package it.generic_service_adapter.domain.model;
