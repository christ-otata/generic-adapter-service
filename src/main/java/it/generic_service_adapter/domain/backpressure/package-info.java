/**
 * {@code BackPressureController} domain service, {@code ListenerControl} and {@code
 * DestinationProbe} ports for the E6 back-pressure (ADR 0007).
 */
package it.generic_service_adapter.domain.backpressure;
