/**
 * Spring wiring only: beans and {@code @ConfigurationProperties} registration, no domain logic (see
 * {@link it.generic_service_adapter.config.properties}).
 */
package it.generic_service_adapter.config;
