/**
 * Multi-cluster Kafka factory (source consumer / destination producer), {@code @RetryableTopic}
 * configuration and {@code MANUAL_IMMEDIATE} ack wiring.
 */
package it.generic_service_adapter.config.kafka;
