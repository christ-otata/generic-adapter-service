/**
 * Typed {@code @ConfigurationProperties} classes, one per configuration area, bound from the
 * per-environment yml files: no hardcoded value anywhere in the application.
 */
package it.generic_service_adapter.config.properties;
