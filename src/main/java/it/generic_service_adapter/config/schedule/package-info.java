/**
 * Enables Spring's {@code @Scheduled} support for the time-driven inbound components in {@code
 * inbound/schedule} ({@code OrphanReprocessor} — WP5; {@code ReportRunner} — later WP).
 */
package it.generic_service_adapter.config.schedule;
