/**
 * Wiring for the orphan-movement domain services that stay Spring-free (ADR 0003): {@code
 * OrphanHoldService} is constructed here rather than component-scanned.
 */
package it.generic_service_adapter.config.orfani;
