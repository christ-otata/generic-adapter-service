/**
 * JSON to internal model to Protobuf translation, isolated and unit-testable. Dependency rule:
 * {@code mapping -> domain.model} only (ADR 0001).
 */
package it.generic_service_adapter.mapping;
